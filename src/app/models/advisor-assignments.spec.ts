import { AdvisorAssignments } from './advisor-assignments';

describe('AdvisorAssignments', () => {
  it('should create an instance', () => {
    expect(new AdvisorAssignments()).toBeTruthy();
  });
});
